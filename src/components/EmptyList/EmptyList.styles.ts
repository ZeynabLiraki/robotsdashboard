import { Box } from "@mui/material";
import styled from "styled-components";

export const EmptyWraper = styled(Box)`
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 250px;
  background-color: #fafafa;
  border-radius: 12px;
  border: 1px dashed #e0e0e0;
  text-align: center;
`;
